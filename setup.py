from setuptools import setup


setup(
    name="xtreegen",
    version="0.1.0",
    packages=["xtreegen"],
    install_requires=[
        "tk",
        "pytest",
        "black",
    ],
)
